# Как добавить админов в систему Minutka

В системе Minutka есть три типа админов:
1. **Супер-админы** - полный доступ ко всей системе
2. **Повара (Chefs)** - управляют рестораном и создают админов ресторана
3. **Админы ресторанов** - управляют данными ресторана

---

## 1. Как добавить супер-админа

### Способ 1: Через Supabase SQL Editor (рекомендуется)

1. **Получите Telegram ID**:
   - Откройте бота Minutka в Telegram
   - Нажмите кнопку "🆔 Chat ID"
   - Скопируйте ваш Chat ID (число, например: `123456789`)

2. **Добавьте через Supabase**:
   - Откройте [Supabase Dashboard](https://app.supabase.com)
   - Перейдите в **SQL Editor**
   - Выполните SQL запрос:

```sql
INSERT INTO super_admins (telegram_id, username, first_name, last_name, is_active)
VALUES 
    (YOUR_TELEGRAM_ID, 'your_username', 'Your', 'Name', true)
ON CONFLICT (telegram_id) DO UPDATE
SET 
    username = EXCLUDED.username,
    first_name = EXCLUDED.first_name,
    last_name = EXCLUDED.last_name,
    is_active = EXCLUDED.is_active;
```

**Пример:**
```sql
INSERT INTO super_admins (telegram_id, username, first_name, last_name, is_active)
VALUES 
    (123456789, 'admin_user', 'Иван', 'Иванов', true)
ON CONFLICT (telegram_id) DO UPDATE
SET 
    username = EXCLUDED.username,
    first_name = EXCLUDED.first_name,
    last_name = EXCLUDED.last_name,
    is_active = EXCLUDED.is_active;
```

3. **Войдите в систему**:
   - Откройте сайт Minutka
   - Нажмите "🔐 Kirish"
   - Введите ваш Telegram ID
   - Вы будете перенаправлены в супер-админ панель

---

## 2. Как добавить повара (Chef)

### Через супер-админ панель:

1. Войдите как супер-админ
2. Перейдите в раздел **"Повара"** (`/admin/chefs`)
3. Нажмите кнопку **"+ Добавить повара"**
4. Заполните форму:
   - **Ресторан** - выберите ресторан
   - **Telegram ID** - ID повара в Telegram
   - **Telegram Chat ID** - Chat ID для получения уведомлений о заказах (можно узнать в боте, нажав "🆔 Chat ID")
   - **Username** (опционально)
   - **Имя** (опционально)
   - **Фамилия** (опционально)
5. Нажмите **"Сохранить"**

**Важно:** `telegram_chat_id` используется для отправки уведомлений о новых заказах повару.

---

## 3. Как добавить админа ресторана

### Способ 1: Через супер-админ панель

1. Войдите как супер-админ
2. Перейдите в раздел **"Админы ресторанов"** (`/admin/restaurant-admins`)
3. Нажмите кнопку **"+ Добавить админа"**
4. Заполните форму:
   - **Ресторан** - выберите ресторан
   - **Telegram ID** - ID админа в Telegram
   - **Username** (опционально)
   - **Имя** (опционально)
   - **Фамилия** (опционально)
5. Нажмите **"Сохранить"**

### Способ 2: Через панель повара

1. Войдите как повар (chef)
2. Перейдите в раздел **"Админы"** (`/restaurant-admin/admins`)
3. Нажмите кнопку **"+ Добавить админа"**
4. Заполните форму:
   - **Telegram ID** - ID админа в Telegram
   - **Username** (опционально)
   - **Имя** (опционально)
   - **Фамилия** (опционально)
5. Нажмите **"Сохранить"**

**Примечание:** Повар может создавать админов только для своего ресторана.

---

## 4. Как получить Telegram ID

### Для пользователей:

1. Откройте бота Minutka в Telegram
2. Нажмите кнопку **"🆔 Chat ID"** в главном меню
3. Бот покажет ваш Chat ID

### Альтернативный способ:

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот покажет ваш User ID (это и есть Telegram ID)

---

## 5. Проверка добавления

### Проверить супер-админов:
```sql
SELECT * FROM super_admins WHERE is_active = true;
```

### Проверить поваров:
```sql
SELECT * FROM chefs WHERE is_active = true;
```

### Проверить админов ресторанов:
```sql
SELECT * FROM restaurant_admins WHERE is_active = true;
```

---

## 6. Управление через веб-интерфейс

После добавления админов через SQL или API, вы можете:

- **Просматривать** список админов в соответствующих разделах панели
- **Редактировать** данные админов
- **Деактивировать** админов (установить `is_active = false`)
- **Удалять** админов (через API или SQL)

---

## Важные замечания

1. **Telegram ID обязателен** - без него админ не сможет войти в систему
2. **Telegram Chat ID для поваров** - необходим для получения уведомлений о заказах
3. **Уникальность** - один Telegram ID может быть только одним типом админа
4. **Приоритет ролей**: Супер-админ > Повар > Админ ресторана > Пользователь

---

## Примеры SQL запросов

### Добавить несколько супер-админов:
```sql
INSERT INTO super_admins (telegram_id, username, first_name, last_name, is_active)
VALUES 
    (123456789, 'admin1', 'Иван', 'Иванов', true),
    (987654321, 'admin2', 'Мария', 'Петрова', true),
    (555666777, 'admin3', 'Алексей', 'Сидоров', true)
ON CONFLICT (telegram_id) DO UPDATE
SET 
    username = EXCLUDED.username,
    first_name = EXCLUDED.first_name,
    last_name = EXCLUDED.last_name,
    is_active = EXCLUDED.is_active;
```

### Деактивировать админа:
```sql
UPDATE super_admins 
SET is_active = false 
WHERE telegram_id = 123456789;
```

### Активировать админа:
```sql
UPDATE super_admins 
SET is_active = true 
WHERE telegram_id = 123456789;
```


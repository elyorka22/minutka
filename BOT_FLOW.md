# Flow Telegram-бота - Детальное описание

## Сценарий 1: Пользователь делает заказ

### Шаг 1: Инициализация
```
Пользователь: /start
  ↓
Бот: Приветствие + список ресторанов (inline keyboard)
```

**Код**: `bot/src/handlers/start.ts`
- Получает список ресторанов из API
- Создает inline клавиатуру
- Отправляет сообщение пользователю

### Шаг 2: Выбор ресторана
```
Пользователь: Нажимает кнопку с рестораном
  ↓
Бот: Сохраняет выбор в сессии
  ↓
Бот: "Опишите ваш заказ в свободной форме"
```

**Код**: `bot/src/handlers/restaurant.ts`
- Обрабатывает callback_query
- Сохраняет `restaurant_id` в сессии
- Запрашивает описание заказа

### Шаг 3: Ввод заказа
```
Пользователь: "Пицца Маргарита, 2 порции, кола 0.5л"
  ↓
Бот: Сохраняет текст заказа в сессии
  ↓
Бот: "Отправьте адрес доставки или геолокацию"
  ↓
Бот: Показывает кнопку "📍 Отправить геолокацию"
```

**Код**: `bot/src/handlers/order.ts`
- Обрабатывает текстовое сообщение
- Сохраняет `orderText` в сессии
- Запрашивает адрес/геолокацию

### Шаг 4: Отправка адреса
```
Пользователь: Отправляет геолокацию ИЛИ адрес текстом
  ↓
Бот: Создает/обновляет пользователя в БД
  ↓
Бот: Создает заказ через Backend API
  ↓
Бот: Отправляет заказ ресторану
  ↓
Бот: Сохраняет telegram_message_id в БД
  ↓
Бот: "Заказ создан! Ожидайте подтверждения..."
```

**Код**: `bot/src/handlers/location.ts`
- Обрабатывает location или текстовый адрес
- Создает пользователя (если не существует)
- Создает заказ через API
- Отправляет уведомление ресторану
- Уведомляет пользователя

## Сценарий 2: Ресторан обрабатывает заказ

### Шаг 1: Получение заказа
```
Бот отправляет сообщение ресторану:
  ↓
"📋 Новый заказ
🆔 Заказ: #abc12345
👤 Клиент: @username
📝 Заказ: Пицца Маргарита, 2 порции
📍 Адрес: ул. Ленина, 10

[✅ Принять] [❌ Отказ]
[🚀 Готово]"
```

**Код**: `bot/src/services/restaurantNotification.ts`
- Получает `telegram_chat_id` ресторана
- Формирует сообщение с деталями заказа
- Создает inline клавиатуру с действиями
- Отправляет сообщение

### Шаг 2: Ресторан принимает заказ
```
Ресторан: Нажимает "✅ Принять"
  ↓
Бот: Обновляет статус через API (pending → accepted)
  ↓
Бот: Логирует изменение в order_status_history
  ↓
Бот: Обновляет сообщение для ресторана
  ↓
Бот: Уведомляет пользователя "✅ Ваш заказ принят!"
```

**Код**: `bot/src/handlers/orderStatus.ts`
- Обрабатывает callback_query с действием
- Обновляет статус через API
- Обновляет сообщение для ресторана
- Уведомляет пользователя

### Шаг 3: Ресторан готовит заказ
```
Ресторан: Нажимает "🚀 Готово"
  ↓
Бот: Обновляет статус (accepted → ready)
  ↓
Бот: Уведомляет пользователя "🚀 Ваш заказ готов!"
```

### Шаг 4: Отказ от заказа
```
Ресторан: Нажимает "❌ Отказ"
  ↓
Бот: Обновляет статус (pending/accepted → cancelled)
  ↓
Бот: Уведомляет пользователя "❌ Заказ отменен"
```

## Сценарий 3: Уведомления пользователя

### Статусы и сообщения

| Статус | Сообщение пользователю |
|--------|------------------------|
| `accepted` | ✅ Ваш заказ принят! Ресторан начал готовить ваш заказ. |
| `ready` | 🚀 Ваш заказ готов! Ожидайте доставку. |
| `cancelled` | ❌ Заказ отменен. Ресторан не может выполнить ваш заказ. |
| `delivered` | ✅ Заказ доставлен! Приятного аппетита! |

**Код**: `bot/src/services/userNotification.ts`
- Получает `telegram_id` пользователя из БД
- Отправляет уведомление о смене статуса

## Структура данных в сессии

```typescript
{
  selectedRestaurantId: string,
  selectedRestaurantName: string,
  orderText: string
}
```

**Примечание**: В production рекомендуется использовать Redis для хранения сессий вместо контекста бота.

## Обработка ошибок

### Ошибки при создании заказа
- Бот уведомляет пользователя
- Предлагает начать заново: `/start`

### Ошибки при отправке ресторану
- Логируется в консоль
- Заказ сохраняется в БД, но ресторан не получает уведомление
- Можно реализовать retry механизм

### Ошибки при уведомлении пользователя
- Логируется в консоль
- Не блокирует обновление статуса

## Команды бота

- `/start` - Начать заказ (показать список ресторанов)
- `/restaurants` - Альтернативная команда для списка ресторанов

## Callback Data Format

- `restaurant:{restaurant_id}` - Выбор ресторана
- `order:accept:{order_id}` - Принять заказ
- `order:ready:{order_id}` - Заказ готов
- `order:cancel:{order_id}` - Отменить заказ

## Интеграция с Backend API

Бот использует Backend API для:
- Получения списка ресторанов: `GET /api/restaurants`
- Получения деталей ресторана: `GET /api/restaurants/:id`
- Создания заказа: `POST /api/orders`
- Обновления статуса: `PATCH /api/orders/:id/status`

**Код**: `bot/src/config/api.ts`


